[build]
  base = "paper-rings-inc"
  command = "npm run build"
  # This matches the structure shown in your successful log
  publish = ".next/server/app"

[[plugins]]
  package = "@netlify/plugin-nextjs"